import styled from "styled-components";

export const LineSvg = styled.div`
  svg {
    position: absolute;
    display: block;
    height: 50px;
    bottom: -49px;
    width: auto;
    left: 50%;
    transform: translateX(-50%);
    z-index: 1;
  }
`;
